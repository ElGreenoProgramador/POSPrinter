<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The printer service is exposed by the hardware system app.
         No special permission is required to bind to it on POSPrinter hardware, but READ_MEDIA / storage
         permissions are needed so the user can pick a photo or video to print. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Declares that this app talks to the printer service (helps some OS builds
         surface the binding correctly; harmless on generic devices, where the app will just
         report "no printer found"). -->
    <queries>
        <package android:name="woyou.aidlservice.jiuiv5" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.POSPrinter"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ImageAdjustActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.POSPrinter" />

        <activity
            android:name=".VideoFramePickerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.POSPrinter" />

        <activity
            android:name=".TextAdjustActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.POSPrinter"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.POSPrinter" />

    </application>

</manifest>
